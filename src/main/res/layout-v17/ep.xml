<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android">

    <TextView
        android:textAppearance="@style/h3"
        android:ellipsize="end"
        android:layout_gravity="start|center"
        android:id="@id/snackbar_text"
        android:paddingLeft="@dimen/cr"
        android:paddingTop="@dimen/cs"
        android:paddingRight="@dimen/cr"
        android:paddingBottom="@dimen/cs"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="@integer/l"
        android:layout_weight="1.0"
        android:textAlignment="viewStart"/>

    <Button
        android:textColor="?d8"
        android:layout_gravity="end|center"
        android:id="@id/snackbar_action"
        android:paddingLeft="@dimen/cr"
        android:paddingTop="@dimen/cs"
        android:paddingRight="@dimen/cr"
        android:paddingBottom="@dimen/cs"
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/co"
        android:layout_marginStart="@dimen/co"
        style="?attr/borderlessButtonStyle"/>
</merge>