<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:background="#fff8f8f8"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                xmlns:android="http://schemas.android.com/apk/res/android">

    <TextView
        android:textSize="@dimen/em"
        android:textColor="@color/da"
        android:gravity="center_horizontal"
        android:id="@id/tv_no_coupon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/xo"
        android:drawableTop="@drawable/a5m"
        android:drawablePadding="15.0dip"
        android:layout_centerInParent="true"/>

    <ListView
        android:id="@id/lv_coupon"
        android:visibility="gone"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:divider="@color/in"
        android:dividerHeight="@dimen/di"/>
</RelativeLayout>